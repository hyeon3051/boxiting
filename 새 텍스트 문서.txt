git add .(편집한 파일을 스테이지에 올린다)
git commit(스테이지 파일에 어떤 이름으로 올릴지 정한다)
git log(commit 변경 사항을 추적한다)
git push (commit을 github 사이트에 올린다)
git pull( commit 변경 사항을 받는다)
git clone(말 그대로 복사본을 만든다)
git 
git branch cat(지금 시점으로 cat이라는 branch를 만든다)
git checkout cat (cat이라는 branch로 이동한다.)